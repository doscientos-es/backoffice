// No-op stub for marker packages that have no runtime behaviour outside the
// Next.js bundler (e.g. `server-only`, `client-only`). Aliased in
// vitest.config.ts so modules guarded by these imports load under test.
export {};
